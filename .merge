#!/bin/bash
DIR="`dirname "$0"`"
HOST="schemu"
PORT=3001

docker build --tag $HOST:1.0 ../
docker stop $HOST
docker rm $HOST
docker run --publish $PORT:3000 --detach --restart=always --name $HOST $HOST:1.0

sudo /usr/sbin/service nginx restart

echo "merged!"